import React from 'react';
import RegisterData from './RegisterData';

const Register = () => {
    document.title = 'Iths Dog - Register'
    return (
        <React.Fragment>
            <RegisterData></RegisterData>
        </React.Fragment>
    );
}

export default Register;